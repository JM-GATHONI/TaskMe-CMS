/**
 * User App Portal — Investors
 * Reuses the R-REIT Investor Dashboard so portfolio, charts, and referrals
 * stay in sync with live DataContext data (no duplicate mock implementation).
 */
export { default } from '../r-reits/InvestorDashboard';
